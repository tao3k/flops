#pretty
{
  config = {
    services = {
      openssh = {
        enable = true;
        kPop = {
          __meta__ = {
            defaults = { };
            extension = <function>;
            name = "kPop";
            precedenceList = [ ];
            supers = [ ];
          };
          a = "b";
        };
      };
    };
  };
  imports = [ ];
  options = {
    services = {
      openssh = {
        description = ''
          Options to pass to the import.
        '';
      };
    };
  };
}